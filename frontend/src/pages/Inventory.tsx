// Placeholder — the real inventory table (additive-upsert add, inline PATCH
// rules, match_name editor) lands in Phase 4 (docs/frontend/spec.md §10.9).
export default function Inventory() {
  return <h1>Inventory</h1>;
}
