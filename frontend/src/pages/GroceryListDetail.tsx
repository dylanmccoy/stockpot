// Placeholder — the real shopping screen (optimistic check/uncheck, atomic
// quantity+unit edit, submit dialog, archive) lands in Phase 6
// (docs/frontend/spec.md §10.7).
export default function GroceryListDetail() {
  return <h1>Grocery list</h1>;
}
